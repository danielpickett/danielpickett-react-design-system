import React from 'react'
import './.css'

export const : React.FunctionComponent<{onClick: () => void}> = ({ children, onClick }) => {
  return (
    <div className="" onClick={onClick}>
      
    </div>
  )
}
